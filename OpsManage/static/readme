静态文件夹使用说明：

1.[dist]
  用于存放发布的JS/CSS组件，
  如果已发布的组建为文件，直接将该文件夹放入本目录，
  如果组建为单个文件，根据文件类型放入JS/CSS/等目录。

2.[build]
  用于存放项目构建的JS/CSS文件，
  公用JS/CSS文件直接放在本目录下，命名规则：功能描述.js  功能描述.css
  页面专用/JS/CSS文件，建立相应页面的文件夹，文件存放在页面文件夹下，命名规则为 appname_webname.js appname_webname.css

3.[fonts]
   用于存放字体文件。

4.[resources]
  用于存放上传、项目生成的文件,
  每个app建立专用文件夹，在app文件夹下根据文件类型建立img doc media等文件夹。
